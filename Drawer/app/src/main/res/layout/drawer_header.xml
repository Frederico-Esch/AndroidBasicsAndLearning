<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="176dp"
    android:orientation="vertical"
    android:background="@drawable/drawer_card_gradient_background"
    android:gravity="bottom"
    android:padding="16dp"
    android:id="@+id/drawer_header_layout">
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/perfil_icon_24"
        android:minHeight="50dp"
        android:minWidth="50dp"
        android:maxWidth="100dp"
        android:maxHeight="100dp"/>
    <TextView
        android:id="@+id/drawer_header_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Place Holder Name"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/gray"/>
    <TextView
        android:id="@+id/drawer_header_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PLACE HOLDER EMAIL"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/gray"/>

</LinearLayout>